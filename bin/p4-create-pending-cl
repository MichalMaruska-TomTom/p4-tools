#! /bin/bash -eu
set -o pipefail

usage()
{
    cat <<EOF
$0 [message]
EOF
}

if [ $# != 1 ]
then
    usage
    exit -1
fi


${P4SHARE:-~/tomtom/}/new-changelist-template "$1" |\
p4 change -i |\
sed -e 's/Change \([0-9]\+\) created\./\1/'
